# plugins

Native OBS plugins (C++). Empty on purpose.

Only build one when a browser source genuinely cannot do the job — custom video filters,
capture sources, audio processing. Requires CMake (`brew install cmake`) and the official
OBS plugin template.
