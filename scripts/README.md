# scripts

OBS Lua/Python scripts, loaded inside OBS via **Tools ▸ Scripts**.

For small in-OBS automations that don't justify a separate process. Anything that needs
network access or state across sessions belongs in the bridge instead.
